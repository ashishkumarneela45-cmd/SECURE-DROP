{
  "name": "securedrop-backend",
  "version": "1.0.0",
  "description": "SecureDrop backend - stores only encrypted file blobs, never plaintext",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "uuid": "^9.0.0"
  }
}
